{
  "name": "multi-step-form",
  "version": "1.0.0",
  "description": "Multi-step form",
  "main": "scripts/main.js",
  "scripts": {
    "compile": "npx sass sass/main.scss:css/main.css --style compressed",
    "watch": "npx sass --watch sass/main.scss:css/main.css --style compressed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Álvaro",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.71.1"
  }
}
